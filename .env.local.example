# Local MCP Server Configuration
# Copy this file to .env and configure for local deployment

# Deployment Mode (auto-detected, but you can force it)
MCP_DEPLOYMENT_MODE=local

# Authentication - Choose ONE method:
# Option 1: Company API Key (for admin/HR access)
PASSGAGE_API_KEY=your_company_api_key_here

# Option 2: User Credentials (for personal access)  
# PASSGAGE_USER_EMAIL=your.email@company.com
# PASSGAGE_USER_PASSWORD=your_secure_password

# API Configuration
PASSGAGE_BASE_URL=https://api.passgage.com
PASSGAGE_TIMEOUT=30000
PASSGAGE_DEBUG=false

# Transport (auto-set to 'stdio' for local mode)
# TRANSPORT_TYPE=stdio

# Local Mode - No additional configuration needed
# Security and session features are disabled for local deployment

# Logging
LOG_LEVEL=info
LOG_FORMAT=pretty
NODE_ENV=development

# Notes:
# - Local mode uses stdio transport for Claude Desktop integration
# - No session management or security features are enabled
# - Direct user authentication with Passgage API
# - Suitable for individual users or development